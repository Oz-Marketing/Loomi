<!--
  Optional: add a `## Changelog` block below and merging this PR files a DRAFT
  changelog entry in Loomi. Nothing is visible and nobody is emailed until
  someone presses Publish on /changelog.

  Leave it out for plumbing, refactors, CI and anything else a user wouldn't
  notice — most PRs shouldn't produce a release note.

  Fields:
    type      feature | improvement | fix     (default: improvement)
    audience  everyone | staff                (default: everyone)
    title     one line, required
    body      everything after the blank line, required

  `audience: everyone` is seen by clients — write it for a dealer, not for us.
  Several entries in one PR: separate them with a line of ---

  Example:

  ## Changelog
  type: feature
  audience: everyone
  title: Bulk download in the Asset Library

  Select any number of assets and download them as a single zip. Large
  selections stream as they're built, so there's no wait on a progress bar.
-->

## What changed

## Why
