# dependencies
node_modules/

# next.js
.next/
out/

# build
tsconfig.tsbuildinfo

# preview cache
.preview-cache/

/src/generated/prisma

# local environment and secrets
.env*
!.env.example

# local databases
prisma/dev.db
prisma/dev.db-*

# local editor/app metadata
.DS_Store
.claude/
.vscode/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Locally-cropped EVOX jellybeans written by scripts/tmp-seed-honda-run.ts.
# Test fixtures for the offer-email preview, regenerated on demand — never commit.
public/tmp-jellybeans/

# Throwaway seed/diagnostic scripts written during a session. Real seeds are
# named `seed-*` / `backfill-*` and are wired into deploy:prepare.
scripts/tmp-*.ts
