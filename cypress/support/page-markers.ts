/**
 * Text rendered by src/app/error.tsx and src/app/not-found.tsx, for specs that
 * assert a page didn't die. Matched as regexes because both use a typographic
 * apostrophe (&rsquo;).
 */
export const ERROR_BOUNDARY = /This page didn.t load/;
export const NOT_FOUND = /We couldn.t find that page/;
