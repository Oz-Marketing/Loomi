// The editor itself lives in components/users so the Agency Settings modal can
// embed it — a page.tsx may export only a default plus Next's own fields.
export { UserDetailPage as default } from '@/components/users/user-detail';
